export type newTopicInput = {
  title: string;
  slug: string;
};

type topicId = string;

export type Params = {
  topicId: topicId;
};

export type inputUpdateTopic = {
  title: string;
  slug: string;
};
